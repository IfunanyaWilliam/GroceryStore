namespace GroceryStore
{
    class Program
    {

        static void Main()
        {
            //var store = new Store();
            //Product product = new Product("Ijebu Garri") { Price = 3000m }; //m behind the 3000 is to identify the type as decimal
            //store.Products.Add(product);                                    //Add instantiated product to the store
            //CheckOut  discountedPrice = new CheckOut(store);                //Parse the store to CheckOut to calculate the rest

            //decimal discount = discountedPrice.CalculateDiscount(product.Id, 0.08);
            //Console.WriteLine("Original Price: {0}", product.Price);
            //Console.WriteLine("Original Price: {0}", discount);
            //Console.WriteLine("The Product ID: {0}", product.Id);

            //var id = product.Id;

            
            
        }
    }

}


